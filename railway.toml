[build]
builder = "NIXPACKS"

[deploy]
startCommand = "streamlit run wiki_dashboard_main.py --server.port $PORT --server.address 0.0.0.0"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"

[environments.production]
variables = { } 